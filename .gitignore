# [HARD] 這個 repo 是 patch-only：只放譯文、碼表、字型、工具、文件與引擎 patch。
# 遊戲本體、手冊掃描、MT-32 ROM、含原版配樂的影片一律不入庫。

# --- 版權素材，絕不入庫 ---
# 倚天中文系統的原始字庫（第三方商業資產）。烘出來的 cht-data/rex_big5.fnt
# 只含譯文用到的 2394 字，是衍生物，那個可以進。
workplace/font-src/
*.ROM
*.zip
*.pdf
workplace/game/
workplace/mt32-rom/
!workplace/build/mingw-tree/gui/themes/*.zip

# --- 遊戲資料 ---
# [雷] gitignore 的比對區分大小寫。原本只寫了大寫 *.RES，結果 AppImage staging 裡的
# rexopen.res / rexend1-3.res（小寫）整組躲過，被 leak-scan 在 push 前抓到。
# 兩種大小寫都列，別靠記憶挑一種。
*.hag
*.HAG
*.res
*.RES
[aipRrx]sound.*
[AIPRX]SOUND.*
digital.aga
DIGITAL.AGA

# --- 打包 staging 與快取（含 build 出來的 binary、系統 .so、appimagetool）---
workplace/tools/pkg/build/
workplace/tools/pkg/cache/

# --- workplace/out/ 一律不入庫 ---
#
# [HARD] 這裡整個目錄 ignore，不要改回逐項列舉。
# 原本是一行一個子目錄（out/audio/、out/promo/、out/win-verify/…），
# 每新增一種產物就要記得補一行 —— 2026-08-07 一口氣多了 gp-sync2/ gp-sync3/
# intro/ intro2/ attract/ anim/ animverify/ 七個目錄，**七個全部漏掉**，
# 而它們裝的是遊戲截圖與錄影（MicroProse 的美術與配樂）。
# 「忘記加一行」的代價是版權素材進公開 repo，這個預設值不能是「收錄」。
#
# 真的要讓某個檔進版控，用 ! 明確開例外，讓它顯眼。
workplace/out/

*.mp4
*.wav
*.raw

# --- build 產物與大型中間物 ---
workplace/scummvm-src/
workplace/build/
workplace/dist-all/
dist-all/
*.o
*.a
*.d
*.dwo
.deps/

# --- 截圖：只有 docs/img/ 底下的進版控（workplace/out/ 已整個 ignore）---

# --- 雜項 ---
__pycache__/
*.pyc
.DS_Store
